# Streamlit Community Cloud reads this file. The app reads the committed
# reports/*.jsonl directly AND drives the real scoring engine, so it installs
# the local package (`.`) to import grid_silicon.scoring.compute_realness_score.
streamlit>=1.40
.
